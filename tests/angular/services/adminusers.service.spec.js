ngDescribe({
    name: 'Test AdminusersService',
    modules: 'app',
    inject: 'AdminusersService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
