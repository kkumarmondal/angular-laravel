ngDescribe({
    name: 'Test event-hash-add component',
    modules: 'app',
    element: '<event-hash-add></event-hash-add>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
